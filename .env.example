# BlueSkyz Labs Web — public environment template only. Never commit secrets.
# ADR 0006 defines the canonical organizational origin. Contact/security emails
# remain empty until verified owner facts exist.
# Promotion evidence must bind the settled exact head; helper-generated commits are not final CI evidence.

PUBLIC_SITE_URL="https://blueskyzlabs.com"
PUBLIC_CONTACT_EMAIL=""
PUBLIC_SECURITY_EMAIL=""
