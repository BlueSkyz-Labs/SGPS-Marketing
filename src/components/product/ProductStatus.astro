---
interface Props {
  label: string;
}
const { label } = Astro.props;
---

<span
  data-product-status={label}
  class="inline-flex rounded-[var(--radius-card)] border border-[var(--border-subtle)] px-3 py-1 text-xs font-semibold tracking-wide text-[var(--action-primary)]"
>
  {label}
</span>
