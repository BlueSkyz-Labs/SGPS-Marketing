---
import BaseLayout from "@/layouts/BaseLayout.astro";
import Container from "@/components/layout/Container.astro";
import PageHeader from "@/components/layout/PageHeader.astro";
import ProductCard from "@/components/product/ProductCard.astro";
import ProofFirstEmptyState from "@/components/product/ProofFirstEmptyState.astro";
import { getPublicProducts } from "@/lib/products";
import IntentLens from "@/components/experience/IntentLens.astro";
import Atlas from "@/components/experience/Atlas.astro";

const products = await getPublicProducts();
const emptyDescription = products.length
  ? "Explore BlueSkyz Labs products with truthful status and evidence."
  : "Products are published only with verifiable public evidence.";
---

<BaseLayout
  title="Products"
  description={emptyDescription}
  path="/en/products/"
  surface="products"
>
  <section class="py-16 lg:py-20">
    <Container>
      <PageHeader
        title="Products you can verify"
        lede="Products appear here when they are ready to evaluate. We publish only what you can verify."
      />
      {
        products.length === 0 ? (
          <div class="mt-12">
            <ProofFirstEmptyState lang="en" />
          </div>
        ) : (
          <div class="mt-12 grid gap-4 md:grid-cols-2">
            {products.map((product) => (
              <ProductCard product={product} lang="en" />
            ))}
          </div>
        )
      }
    </Container>
  </section>
  {
    /* C2: discovery surfaces re-homed from the homepage (design §8) — Intent Lens sits
      with product evaluation; Atlas remains the deliberate exploration tool. */
  }
  <section class="border-t border-[var(--border-subtle)] py-16 lg:py-20">
    <Container>
      <IntentLens lang="en" />
      <Atlas lang="en" />
    </Container>
  </section>
</BaseLayout>
