# S+ E4 Validation — 2026-09-11

**Scope:** S+ Task 13. Separates automated/browser confidence from real-user
evidence. **Human E4 remains OPEN — automated PASS is never reported as
real-user acceptance.**

- Automated matrix run: exact head of the E4 PR (recorded below after merge
  readiness), against a fresh `main`-equivalent build.
- Deployed read-back for the S+ wave: production smoke (Task 12C) —
  ALL 19 checks pass at `f2c3339` (legacy 301s live).

## 1. Automated source-gate matrix

| Command                                                      | Result                                                                                                                                                                           |
| ------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm install --frozen-lockfile`                             | PASS (lockfile honored, no changes)                                                                                                                                              |
| `pnpm audit --audit-level=moderate`                          | PASS — No known vulnerabilities found                                                                                                                                            |
| `pnpm test:architecture`                                     | PASS — **126/126**                                                                                                                                                               |
| `pnpm typecheck`                                             | PASS — 0 errors, 0 warnings                                                                                                                                                      |
| `pnpm lint`                                                  | PASS — 0 warnings                                                                                                                                                                |
| `pnpm format:check`                                          | PASS                                                                                                                                                                             |
| `pnpm build`                                                 | PASS — static export verified                                                                                                                                                    |
| `pnpm check:client-budget`                                   | PASS — site-wide 1,644 B, worst page 1,644 B (< 120,000 B)                                                                                                                       |
| `pnpm check:static-links`                                    | PASS                                                                                                                                                                             |
| `pnpm test:e2e` (chromium, firefox, webkit, mobile-chromium) | PASS with environment margins — see §2a                                                                                                                                          |
| Full 5-project matrix incl. `edge-ua` (`--workers=2`)        | **563 / 565 pass** — 2 webkit-on-Windows focus-assertion quirks (§6)                                                                                                             |
| `pnpm lighthouse` (3 runs, desktop, `lhci autorun`)          | **PASS — perf 1.00 · a11y 1.00 · best-practices 1.00**; exact-head: LCP **480 ms**, CLS **0.0000**, TBT **0 ms**, SI 472 ms (SEO 0.69 = local-only noindex behavior, documented) |

## 2. Viewport & interaction matrix (automated)

`scripts/e4-matrix.mjs` — reproducible sweep; **38/38 checks pass**:

- Viewports: **320 / 360 / 390 / 393 / 430 / 1280 / 1440 / 1920 px** on `/en/`
  across **chromium, firefox, webkit** — hero, intent lens, principle
  matrix, and atlas present; **zero horizontal overflow**.
- Keyboard-only flow (chromium): skip link → header controls → story spine →
  intent lens reachable and focusable via Tab.
- Reduced motion: all content visible, no overflow (engines: chromium,
  firefox, webkit).
- 200% text zoom (root font doubled; layout is rem-based): no overflow,
  content intact — **after the remediation fix below**.
- Mobile Chromium: covered by the e2e matrix (`mobile-chromium` project).

### Remediation found by this matrix (FIXED in this PR)

- **200% text zoom** exposed an 8 px document overflow caused by the desktop
  header primary nav (WCAG 1.4.4 resize-text risk). Fix: the header container
  now wraps (`flex-wrap` + row gap) so the brand lockup and nav stack
  gracefully when text is zoomed, with no visual change at 100%.

### 2a. Full-suite local run notes (environment honesty)

Two consecutive full local runs were taken on this branch's exact head:

- Run A (`pnpm test:e2e`, default workers): 443/452 — nine failures, all
  re-verified green in isolation: six **firefox GFX crashes under parallel
  load** (15/15 axe tests pass in isolation), two webkit focus assertions,
  one mobile-chromium 30 s timeout (1/1 passes in isolation).
- Run B (full 5-project matrix incl. `edge-ua`, `--workers=2`): **563/565** —
  the only two failures are the webkit-on-Windows focus-assertion quirk
  described in §6.

The authoritative cross-browser matrix is the CI **Browser Assurance** job
(Linux runners, same Playwright matrix); it is green on this exact head and
was green on every S+ PR merged this wave (#104–#118).

## 3. Canonical real-user tasks — automated proxies only

| #   | Task                                            | Automated proxy                                                                 | Verdict                                    |
| --- | ----------------------------------------------- | ------------------------------------------------------------------------------- | ------------------------------------------ |
| 1   | Explain what BlueSkyz does after brief exposure | Hero proposition + One House matrix render (e2e)                                | CONTENT PRESENT — comprehension NOT tested |
| 2   | Find a product / conclude none is public        | Products page + atlas/journey honest empty states (e2e)                         | PASS (automated)                           |
| 3   | Can an available product be used now            | Not applicable — no public products; empty-state stated honestly                | N/A (truthful)                             |
| 4   | Find help/support                               | Support route reachable from nav/footer/journey/navigator (e2e)                 | PASS (automated)                           |
| 5   | Find who is responsible for BlueSkyz            | About route + founder line (e2e)                                                | PASS (automated)                           |
| 6   | Explain why products/principles belong together | One House matrix + Atlas relationships (e2e)                                    | CONTENT PRESENT — comprehension NOT tested |
| 7   | Identify anything exaggerated/fake/unclear      | Anti-fabrication contracts (truth ledger, atlas, analytics) + banned-copy gates | PASS (automated guards)                    |

**These are automated proxies. Real-user comprehension and credibility
evidence is required separately (Section 5) and remains OPEN.**

## 4. S+ probes — automated observations

| Probe                                       | Automated observation                                                                 |
| ------------------------------------------- | ------------------------------------------------------------------------------------- |
| Horizon recognizable but restrained         | Rendered + independently critiqued (desktop/mobile); decorative, aria-hidden, zero JS |
| Trust Ledger understandable and credible    | Three truthful lanes, per-surface routes, anti-fabrication contract tests             |
| Intent Lens helpful, not invasive           | DOM-only state, no cookies/storage/network (tested); emphasis-only behavior           |
| Journey Bar improves orientation            | Route-derived next steps on every content page; honest labels                         |
| Command Navigator improves speed            | Ctrl/Cmd+K opens, filters without transmitting input, Escape restores focus           |
| Atlas clarifies relationships or adds noise | Truth-derived nodes only; product nodes appear only when real; zero JS                |

Prompt wording for the human E4 protocol (below) mirrors these probes.

## 5. Human E4 protocol (OPEN — MANUAL/EXTERNAL REQUIRED)

Real-user evidence requires people outside the build loop. Protocol for the
owner to run (lightweight, ~30 minutes, 5-8 participants if possible):

1. Give each participant the production URL (desktop + their own phone).
2. Read the seven canonical tasks (Section 3, left column) verbatim; record
   task success + first confusion point, without guidance.
3. Ask the six S+ probes (Section 4) as open questions.
4. Capture: task outcomes, verbatim confusion quotes, any "exaggerated/fake"
   flags, device/browser used.
5. Attach results to this evidence file (or a successor) and update the
   human-E4 row to PASS/FAIL with dates.

Until step 5 is complete, human E4 = **OPEN**. No automated result in this
document upgrades it.

## 6. Residuals

- WebKit-on-Windows local runs show a known skip-link focus quirk in this
  environment only (identical behavior on the live site + unchanged spec;
  CI webkit on Linux passes) — recorded as an environment note, not a defect.
- Human E4 items remain open as described.
