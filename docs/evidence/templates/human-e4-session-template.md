# Human E4 — session record (template)

> Copy to `docs/evidence/YYYY-MM-DD-human-e4-<locale>-<participant-id>.md`.
> One file per participant session. Never invent a participant, quote, or
> score. Status starts at `NOT RUN` and only a real session changes it.

**STATUS: `NOT RUN`** ← keep this line until a real session has run.

**Locale:** `EN` / `VI` · **Participant ID:** `P?` (anonymized — no names)
**Date/time:** `NOT RECORDED` · **Duration:** `NOT RECORDED`
**Moderator (not the implementer):** `NOT RECORDED`
**Device / viewport:** `NOT RECORDED` · **Input method:** `NOT RECORDED`

## Consent

- [ ] Participant read and understood the purpose: evaluate clarity and
      credibility of the public site, not the participant.
- [ ] Participant consented to note-taking. Recording: `NOT RECORDED` /
      consented / declined (if declined, record notes only, no audio/video).
- [ ] Participant consented to anonymized quotes being used in evidence.
- [ ] Participant was told they may stop at any time without reason.
- [ ] No personal data beyond the anonymized ID is stored.

If any consent box is unchecked, do not run the session. Mark `INVALID`.

## Tasks (from `docs/evidence/2026-09-12-v3-human-e4.md`)

Mark each task `DONE` / `BLOCKED` / `SKIPPED` and record only what was seen.

- [ ] 1. Explain a truth state without prompting.
- [ ] 2. Open "Verify this page" and identify a supporting source.
- [ ] 3. Explain "establishes" versus "does not establish".
- [ ] 4. Find and use an Evidence Passport.
- [ ] 5. Compare two public items in Decision Room and explain why there is no
      score or recommendation.
- [ ] 6. Switch EN/VI and retain evidence context.
- [ ] 7. Use Provenance Search to find a security or privacy evidence
      destination.
- [ ] 8. Explain whether "not published" means "does not exist".

Success criteria (from the same protocol): tasks 2 and 4 reached unaided in
under 90 seconds; task 5 states the room compares facts and produces no
verdict; task 8 distinguishes "not published" from "does not exist"; no task
may depend on internal governance vocabulary.

## OBSERVATION (what happened — no interpretation here)

Record behaviour, verbatim quotes, time-on-task, and dead ends. Verbatim or
omitted — never paraphrased into a claim.

| Task | Outcome   | Time | Observation (verbatim / factual) |
| ---- | --------- | ---- | -------------------------------- |
| 1    | `NOT RUN` | `—`  |                                  |
| 2    | `NOT RUN` | `—`  |                                  |
| 3    | `NOT RUN` | `—`  |                                  |
| 4    | `NOT RUN` | `—`  |                                  |
| 5    | `NOT RUN` | `—`  |                                  |
| 6    | `NOT RUN` | `—`  |                                  |
| 7    | `NOT RUN` | `—`  |                                  |
| 8    | `NOT RUN` | `—`  |                                  |

**Raw record:** `NOT RECORDED` (transcript path / note file — required).

## INTERPRETATION (why we think it happened — inference, clearly labelled)

Everything below is the moderator's inference, not participant fact. State the
observation each inference rests on.

| #   | Inference | Rests on (observation ref) | Confidence |
| --- | --------- | -------------------------- | ---------- |
| 1   | `NOT RUN` | `—`                        | `—`        |

## Comprehension and credibility defects

Classify `P0` / `P1` / `P2`. P0/P1 comprehension or credibility defects block
Premium PASS and require re-running the affected task after the fix.

| Severity  | Defect (observed) | Affected task | Follow-up |
| --------- | ----------------- | ------------- | --------- |
| `NOT RUN` |                   |               |           |

## Status

- Session status: **`NOT RUN`** (set to `RUN` only with a real session and its
  raw record linked above; `INVALID` if consent was incomplete).
- Human E4 overall: **OPEN** — this single session never closes it. Closure
  requires the full participant set per locale, per the protocol.
- Automated suites and density proxies may **never** promote this status.
